## Behind the Scene of React JSX

JSX is just an abstraction of DOM methods. 

This very simple HTML file demonstrates the what how React abstract DOM manipulation with ```React.createElement()``` and React JSX


